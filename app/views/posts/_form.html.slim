.scaffold-form
  = simple_form_for(post) do |f|
    .form-inputs
      = f.label "Blog"
      = f.collection_select(:blog_id, current_user.blogs, :id, :name)
      = f.input :title, autofocus: true
      = f.cktext_area :body, cols: 40, ckeditor: { uiColor: "#e6e6e6", toolbar: "mini" }

    .form-actions
      = f.button :submit
      '  or
      = link_to "Back", :back
